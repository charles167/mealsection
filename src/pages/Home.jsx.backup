import { useNavigate } from "react-router-dom";
import useCart from "../hooks/useCart";
import React, { useContext, useState, useEffect, useMemo } from "react";
import { IoIosPricetags } from "react-icons/io";
import { FaStar } from "react-icons/fa";
import { FiUser } from "react-icons/fi";
import { LuDownload, LuRefreshCw } from "react-icons/lu";
import { useAuthContext } from "../context/AuthContext";
import { IoEyeOffOutline, IoEyeOutline } from "react-icons/io5";
// Simple count-up hook for animated numbers
function useCountUp(target, duration = 800) {
  const [value, setValue] = useState(0);
  useEffect(() => {
    const start = performance.now();
    function tick(now) {
      const progress = Math.min((now - start) / duration, 1);
      setValue(Math.floor(progress * target));
      if (progress < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }, [target, duration]);
  return value;
}

function Home() {
  const navigate = useNavigate();
  const { user, vendors, userFetch } = useAuthContext();
  const [open, setOpen] = useState(false);
  const [blur, setBlur] = useState(false);
  const [form, setForm] = useState("");
  const { cart } = useCart();
  const animatedBal = useCountUp(user?.availableBal || 0);

  // Derived stats + recommendations memoized
  const stats = useMemo(() => {
    const total = vendors?.length || 0;
    const active =
      vendors?.filter((v) => v.isActive).length ||
      Math.min(total, Math.round(total * 0.7));
    const cartItems = cart?.length || 0;
    return [
      { label: "Vendors", value: total, color: "from-red-500 to-orange-500" },
      {
        label: "Active",
        value: active,
        color: "from-green-500 to-emerald-500",
      },
      {
        label: "Cart Items",
        value: cartItems,
        color: "from-purple-500 to-pink-500",
      },
      {
        label: "Wallet (₦)",
        value: user?.availableBal || 0,
        color: "from-blue-500 to-cyan-500",
      },
    ];
  }, [vendors, cart, user?.availableBal]);

  const recommendations = useMemo(() => {
    if (!vendors) return [];
    // Simple heuristic: slice first 4 vendors, could be replaced with rating/time logic.
    return vendors.slice(0, 4);
  }, [vendors]);

  const [promoIndex, setPromoIndex] = useState(0);
  const promos = useMemo(
    () => [
      {
        vendor: "Elegance",
        discount: "20%",
        color: "from-red-500 to-orange-500",
        tagline: "Fresh gourmet delight",
      },
      {
        vendor: "FastBite",
        discount: "15%",
        color: "from-purple-500 to-pink-500",
        tagline: "Lightning fast delivery",
      },
      {
        vendor: "QuickMeal",
        discount: "25%",
        color: "from-blue-500 to-cyan-500",
        tagline: "Big flavors, small wait",
      },
    ],
    []
  );

  useEffect(() => {
    const id = setInterval(() => {
      setPromoIndex((i) => (i + 1) % promos.length);
    }, 5000);
    return () => clearInterval(id);
  }, [promos.length]);

  const categories = ["Rice", "Snacks", "Drinks", "Grill", "Vegan"];

  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 via-red-50/10 to-orange-50/10">
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 space-y-8">
        {/* Greeting */}
        <div className="flex items-center justify-between mb-4 animate-fadeIn">
          <div>
            <h1 className="text-2xl md:text-3xl font-bold text-gray-800 ">
              {user?.fullName
                ? `Welcome, ${user.fullName.split(" ")[0]}👋`
                : "Welcome"}
            </h1>
            <p className="text-sm text-gray-500">
              {cart?.length
                ? `You have ${cart.length} item${
                    cart.length === 1 ? "" : "s"
                  } in your cart ready to order.`
                : "Find the best meals and deals today."}
            </p>
          </div>
          <div className="hidden sm:flex gap-2">
            <button
              onClick={() => navigate("/vendors")}
              className="px-4 py-2 rounded-xl text-xs font-semibold bg-gradient-to-r from-red-500 to-orange-500 text-white shadow hover:shadow-md transition-all"
            >
              Explore Vendors
            </button>
            <button
              onClick={() => navigate("/wallet")}
              className="px-4 py-2 rounded-xl text-xs font-semibold bg-white border border-gray-200 text-gray-700 hover:bg-gray-50 transition-all"
            >
              Wallet
            </button>
          </div>
        </div>

        {/* Wallet Balance Card */}
        <div className="glass rounded-3xl p-6 shadow-md border border-white/50 hover:shadow-2xl transition-all duration-300 animate-fadeIn">
          <div className="flex items-start justify-between mb-4">
            <div className="flex-1">
              <p className="text-sm font-medium text-gray-600 mb-1 flex items-center gap-2">
                <span className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                Wallet Balance
              </p>
              <p
                className={`text-3xl font-bold gradient-text mb-1 ${
                  blur && "blur-sm select-none"
                }`}
              >
                ₦{(animatedBal || 0).toLocaleString()}.00
              </p>
              <p className="text-xs text-gray-500">Available to spend</p>
            </div>
          </div>

          <div className="flex items-center gap-3">
            <button
              onClick={() => navigate("/wallet")}
              className="bg-gradient-to-r from-[#9e0505] to-[#c91a1a] text-white px-4 py-3 rounded-xl font-semibold text-[13px] hover:shadow-lg transition-all duration-300 flex items-center justify-center gap-2"
            >
              <LuDownload size={18} />
              <span>Top Up</span>
            </button>

            <button
              onClick={() => setBlur(!blur)}
              className="p-3 bg-gray-100 hover:bg-gray-200 rounded-xl transition-all duration-300 hover:scale-110"
              aria-label={blur ? "Show balance" : "Hide balance"}
            >
              {blur ? (
                <IoEyeOutline size={20} />
              ) : (
                <IoEyeOffOutline size={20} />
              )}
            </button>

            <button
              onClick={() => userFetch()}
              className="p-3 bg-gray-100 hover:bg-gray-200 rounded-xl transition-all duration-300 hover:scale-110 hover:rotate-180"
              aria-label="Refresh"
            >
              <LuRefreshCw size={20} />
            </button>

            <button
              onClick={() => navigate("/profile")}
              className="p-3 bg-gradient-to-br from-red-50 to-orange-50 hover:from-red-100 hover:to-orange-100 rounded-xl transition-all duration-300 hover:scale-110"
              aria-label="Profile"
            >
              <FiUser size={20} className="text-[var(--default)]" />
            </button>
          </div>
        </div>

        {/* Search Bar */}
        <div
          className="relative animate-fadeIn"
          style={{ animationDelay: "100ms" }}
        >
          <div className="relative">
            <input
              type="text"
              value={form}
              onChange={(e) => {
                const value = e.target.value;
                setForm(value);
                setOpen(value.length > 0);
              }}
              placeholder="Search for vendors..."
              className="w-full bg-white border-1 border-gray-200 rounded-2xl px-6 py-4 pl-12 text-sm focus:border-[var(--default)] focus:ring-4 focus:ring-red-50 outline-none transition-all duration-300 shadow-sm hover:shadow"
            />
            <svg
              className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"
              width="20"
              height="20"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
              />
            </svg>
            {form && (
              <button
                onClick={() => {
                  setForm("");
                  setOpen(false);
                }}
                className="absolute right-3 top-1/2 -translate-y-1/2 w-7 h-7 rounded-full bg-gray-100 hover:bg-gray-200 text-gray-600 text-xs font-bold flex items-center justify-center transition"
                aria-label="Clear search"
              >
                ×
              </button>
            )}
          </div>

          {/* Search Results Dropdown */}
          {open && (
            <div className="absolute mt-3 w-full bg-white rounded-2xl shadow-2xl max-h-80 overflow-y-auto z-20 border border-gray-100 animate-scaleIn">
              {vendors
                .filter((item) =>
                  item?.storeName.toLowerCase().includes(form.toLowerCase())
                )
                .map((item, index) => (
                  <div
                    key={item.id}
                    className="flex items-center gap-3 hover:bg-gradient-to-r hover:from-red-50 hover:to-orange-50 p-4 cursor-pointer transition-all duration-200 first:rounded-t-2xl last:rounded-b-2xl border-b border-gray-50 last:border-0"
                    onClick={() => {
                      navigate(`/vendor/${item.vendorId}`);
                      setForm("");
                      setOpen(false);
                    }}
                    style={{ animationDelay: `${index * 30}ms` }}
                  >
                    <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-red-100 to-orange-100 flex items-center justify-center overflow-hidden">
                      <img
                        src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrWAQ2R-cEo2SpkEW3j0LP49tqBUCiJupF3g&s"
                        className="w-full h-full object-cover"
                        alt={item.name}
                      />
                    </div>
                    <div className="flex-1">
                      <p className="font-semibold text-gray-800 text-sm">
                        {item.storeName}
                      </p>
                      <p className="text-xs text-gray-500">
                        Fast delivery • Quality food
                      </p>
                    </div>
                    <svg
                      className="text-gray-400"
                      width="16"
                      height="16"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d="M9 5l7 7-7 7"
                      />
                    </svg>
                  </div>
                ))}

              {vendors.filter((item) =>
                item.storeName.toLowerCase().includes(form.toLowerCase())
              ).length === 0 && (
                <div className="p-8 text-center">
                  <div className="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-3">
                    <svg
                      className="text-gray-400"
                      width="24"
                      height="24"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeWidth={2}
                        d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M12 12h.01M12 22s-8-4-8-10V5l8-3 8 3v7c0 6-8 10-8 10z"
                      />
                    </svg>
                  </div>
                  <p className="text-sm text-gray-500 font-medium">
                    No vendors found
                  </p>
                  <p className="text-xs text-gray-400 mt-1">
                    Try a different search term
                  </p>
                </div>
              )}
            </div>
          )}
        </div>

        {/* Promo Carousel */}
        <div
          className="relative animate-fadeIn"
          style={{ animationDelay: "180ms" }}
        >
          <div className="overflow-hidden rounded-3xl">
            <div
              className="flex transition-transform duration-700"
              style={{ transform: `translateX(-${promoIndex * 100}%)` }}
            >
              {promos.map((promo, i) => (
                <div
                  key={promo.vendor}
                  className={`min-w-full bg-gradient-to-br ${promo.color} p-6 sm:p-8 relative`}
                >
                  <div className="flex flex-col sm:flex-row sm:items-center gap-6">
                    <div className="flex-1">
                      <div className="flex items-center gap-2 mb-3">
                        <IoIosPricetags size={30} className="text-white" />
                        <span className="bg-white/20 backdrop-blur-sm px-3 py-1 rounded-full text-white text-xs font-bold">
                          Limited Offer
                        </span>
                      </div>
                      <h3 className="text-white font-bold text-3xl mb-2">
                        {promo.discount} OFF
                      </h3>
                      <p className="text-white/90 text-sm max-w-md leading-relaxed">
                        {promo.tagline} on your first order from{" "}
                        <span className="font-semibold">{promo.vendor}</span>.
                        Don’t miss out.
                      </p>
                      <button
                        onClick={() => navigate("/vendors")}
                        className="mt-4 px-5 py-2.5 rounded-xl bg-white text-gray-800 text-xs font-semibold shadow hover:shadow-md transition"
                      >
                        Order Now
                      </button>
                    </div>
                    <div className="flex justify-center">
                      <img
                        src="https://www.pngmart.com/files/23/Food-PNG-Isolated-Image.png"
                        alt="Food"
                        className="w-32 h-32 object-contain drop-shadow-2xl floating"
                      />
                    </div>
                  </div>
                  <div className="absolute inset-0 bg-black/15" />
                </div>
              ))}
            </div>
          </div>
          <div className="flex justify-center mt-4 gap-2">
            {promos.map((_, i) => (
              <button
                key={i}
                onClick={() => setPromoIndex(i)}
                className={`w-2.5 h-2.5 rounded-full transition ${
                  promoIndex === i
                    ? "bg-[var(--default)] scale-125"
                    : "bg-gray-300 hover:bg-gray-400"
                }`}
                aria-label={`Go to slide ${i + 1}`}
              ></button>
            ))}
          </div>
        </div>

        {/* Recommendations */}
        {recommendations.length > 0 && (
          <div className="animate-fadeIn" style={{ animationDelay: "250ms" }}>
            <div className="flex items-center justify-between mb-6">
              <div>
                <h2 className="text-2xl font-bold text-gray-800 flex items-center gap-2">
                  <span className="w-1 h-8 bg-gradient-to-b from-[var(--default)] to-orange-500 rounded-full" />
                  Recommended For You
                </h2>
                <p className="text-sm text-gray-500 mt-1 ml-3">
                  Curated picks based on popularity
                </p>
              </div>
            </div>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
              {recommendations.map((vendor, i) => (
                <div
                  key={vendor.id || vendor._id || i}
                  onClick={() => navigate(`/vendor/${vendor._id}`)}
                  className="bg-white rounded-2xl shadow-sm hover:shadow-xl p-4 cursor-pointer transition group border border-gray-100 hover:-translate-y-1"
                >
                  <div className="h-36 rounded-xl overflow-hidden mb-4 bg-gradient-to-br from-red-100 to-orange-100">
                    <img
                      src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrWAQ2R-cEo2SpkEW3j0LP49tqBUCiJupF3g&s"
                      alt={vendor.storeName}
                      className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                    />
                  </div>
                  <h3 className="font-semibold text-gray-800 mb-1 truncate group-hover:text-[var(--default)] transition-colors text-sm">
                    {vendor.storeName}
                  </h3>
                  <div className="flex items-center gap-1 mb-2 text-xs">
                    <FaStar color="#fbbf24" size={12} />
                    <span className="text-gray-600 font-medium">4.5</span>
                    <span className="text-gray-400">•</span>
                    <span className="text-gray-500">10-30 min</span>
                  </div>
                  {String(vendor.Active).toLowerCase() === "true" ? (
                    <span className="px-2 py-1 bg-green-50 rounded-full text-[10px] font-medium text-green-700 flex items-center gap-1 w-fit">
                      <span className="w-1.5 h-1.5 bg-green-500 rounded-full" />{" "}
                      Online
                    </span>
                  ) : (
                    <span className="px-2 py-1 bg-red-50 rounded-full text-[10px] font-medium text-red-700 flex items-center gap-1 w-fit">
                      <span className="w-1.5 h-1.5 bg-red-500 rounded-full" />{" "}
                      Offline
                    </span>
                  )}
                </div>
              ))}
            </div>
          </div>
        )}

        {/* Featured Vendors */}
        <div className="animate-fadeIn" style={{ animationDelay: "300ms" }}>
          <div className="flex items-center justify-between mb-6">
            <div>
              <h2 className="text-2xl font-bold text-gray-800 flex items-center gap-2">
                <span className="w-1 h-8 bg-gradient-to-b from-[var(--default)] to-orange-500 rounded-full"></span>
                Featured Vendors
              </h2>
              <p className="text-sm text-gray-500 mt-1 ml-3">
                Top rated restaurants near you
              </p>
            </div>
          </div>

          <div className="flex overflow-x-auto space-x-5 scrollbar-hide pb-4">
            {vendors?.map((vendor, index) => (
              <div
                key={vendor.id}
                className="flex-none w-64 bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300 cursor-pointer overflow-hidden hover:-translate-y-2 group"
                onClick={() => navigate(`/vendor/${vendor._id}`)}
                style={{ animationDelay: `${index * 50}ms` }}
              >
                <div className="relative h-40 overflow-hidden bg-gradient-to-br from-red-100 to-orange-100">
                  <img
                    src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrWAQ2R-cEo2SpkEW3j0LP49tqBUCiJupF3g&s"
                    alt={vendor.name}
                    className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                  />

                  <div className="absolute top-3 right-3 bg-white/90 backdrop-blur-sm px-3 py-1.5 rounded-full flex items-center gap-1 shadow-lg">
                    <FaStar color="#fbbf24" size={14} />
                    <span className="text-xs font-bold text-gray-800">4.5</span>
                  </div>
                </div>
                <div className="p-4">
                  <h3 className="font-bold text-gray-800 mb-2 text-sm truncate group-hover:text-[var(--default)] transition-colors">
                    {vendor.storeName}
                  </h3>
                  <div className="flex items-center gap-2 text-xs text-gray-500 mb-3">
                    <span className="flex items-center gap-1">
                      <svg
                        className="w-4 h-4"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          strokeLinecap="round"
                          strokeLinejoin="round"
                          strokeWidth={2}
                          d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                        />
                      </svg>
                      10-30 min
                    </span>
                    <span>•</span>
                    {String(vendor.Active).toLowerCase() === "true" ? (
                      <span className="flex items-center gap-1 px-2 py-0.5 bg-green-50 rounded-full">
                        <div className="w-1.5 h-1.5 bg-green-500 rounded-full"></div>
                        <span className="text-green-700 font-medium">
                          Online
                        </span>
                      </span>
                    ) : (
                      <span className="flex items-center gap-1 px-2 py-0.5 bg-red-50 rounded-full">
                        <div className="w-1.5 h-1.5 bg-red-500 rounded-full"></div>
                        <span className="text-red-700 font-medium">
                          Offline
                        </span>
                      </span>
                    )}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* All Vendors */}
        <div className="animate-fadeIn" style={{ animationDelay: "400ms" }}>
          <div className="flex items-center justify-between mb-6">
            <div>
              <h2 className="text-2xl font-bold text-gray-800 flex items-center gap-2">
                <span className="w-1 h-8 bg-gradient-to-b from-[var(--default)] to-orange-500 rounded-full"></span>
                All Vendors
              </h2>
              <p className="text-sm text-gray-500 mt-1 ml-3">
                Explore all available restaurants
              </p>
            </div>
            <button
              onClick={() => navigate("/vendors")}
              className="text-sm font-semibold text-[var(--default)] hover:text-[var(--primary-dark)] flex items-center gap-1 group"
            >
              <span>View All</span>
              <svg
                className="w-4 h-4 group-hover:translate-x-1 transition-transform"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M9 5l7 7-7 7"
                />
              </svg>
            </button>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {vendors?.slice(0, 6).map((vendor, index) => (
              <div
                key={vendor.id}
                className="bg-white rounded-2xl shadow-sm hover:shadow-xl p-4 flex items-center gap-4 cursor-pointer transition-all duration-300 hover:-translate-y-1 group border border-gray-100"
                onClick={() => navigate(`/vendor/${vendor._id}`)}
                style={{ animationDelay: `${index * 50}ms` }}
              >
                <div className="w-20 h-20 rounded-xl bg-gradient-to-br from-red-100 to-orange-100 overflow-hidden flex-shrink-0">
                  <img
                    src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRrWAQ2R-cEo2SpkEW3j0LP49tqBUCiJupF3g&s"
                    alt={vendor.storeName}
                    className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                  />
                </div>
                <div className="flex-1 min-w-0">
                  <h3 className="font-bold text-gray-800 mb-1 truncate group-hover:text-[var(--default)] transition-colors">
                    {vendor.storeName}
                  </h3>
                  <div className="flex items-center gap-1 mb-2">
                    <FaStar color="#fbbf24" size={14} />
                    <span className="text-xs text-gray-600 font-medium">
                      4.5
                    </span>
                    <span className="text-gray-400">•</span>
                    <span className="text-xs text-gray-500">10-30 min</span>
                  </div>
                  {String(vendor.Active).toLowerCase() === "true" ? (
                    <div className="flex items-center gap-1 px-2 py-1 bg-green-50 rounded-full w-fit">
                      <div className="w-1.5 h-1.5 bg-green-500 rounded-full"></div>
                      <span className="text-green-700 font-medium text-xs">
                        Online
                      </span>
                    </div>
                  ) : (
                    <div className="flex items-center gap-1 px-2 py-1 bg-red-50 rounded-full w-fit">
                      <div className="w-1.5 h-1.5 bg-red-500 rounded-full"></div>
                      <span className="text-red-700 font-medium text-xs">
                        Offline
                      </span>
                    </div>
                  )}
                </div>
                <svg
                  className="w-5 h-5 text-gray-400 group-hover:text-[var(--default)] group-hover:translate-x-1 transition-all flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth={2}
                    d="M9 5l7 7-7 7"
                  />
                </svg>
              </div>
            ))}
          </div>
        </div>
      </main>
    </div>
  );
}

export default Home;
